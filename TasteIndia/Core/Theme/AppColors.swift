import SwiftUI

enum AppTheme {
    static let primaryAccent = Color(red: 0.85, green: 0.35, blue: 0.15) // Warm Terracotta / Saffron
    static let secondaryBadge = Color(red: 0.20, green: 0.55, blue: 0.35) // Cardamom Green
    static let surfaceBackground = Color(uiColor: .systemGroupedBackground)
    static let cardBackground = Color(uiColor: .secondarySystemGroupedBackground)
}
